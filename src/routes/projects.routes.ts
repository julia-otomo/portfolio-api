import { Router } from "express";

const projectRouter: Router = Router();

projectRouter.post("");

projectRouter.get("");

projectRouter.patch("/:title");

projectRouter.delete("/:title");

export { projectRouter };
